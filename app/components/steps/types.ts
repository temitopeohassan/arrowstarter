{
  title,
  description,
  category,
  goal,
  threshold,
  maxCap,
  hasMaxCap,
  hasDeadline,
  fundingDeadline,
  deliveryDate,
  fundingIncrements,
  image,
  imagePreview, // <- extra, optional depending on component
}
